<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net462</TargetFramework>
    <RootNamespace>Autofac.Extras.NLog</RootNamespace>
    
    <!-- NUGET -->
    <PackageTags>NLog, Autofac, Logger, ILogger, log, logging</PackageTags>
    <Description>Autofac Module that integrating Autofac and NLog, it supports both constructor and property injection.</Description>
    <Product>Autofac Module that integrating Autofac and NLog, it supports both constructor and property injection.</Product>
    <Version>1.4.0</Version>
    <Authors>ziyasal</Authors>
    <Company>ziyasal</Company>
    <PackageLicenseUrl>https://raw.githubusercontent.com/PagueVeloz/Autofac.Extras.NLog/master/LICENSE</PackageLicenseUrl>
    <PackageProjectUrl>https://github.com/PagueVeloz/Autofac.Extras.NLog</PackageProjectUrl>
    <RepositoryUrl>https://github.com/PagueVeloz/Autofac.Extras.NLog</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Autofac" Version="4.4.0" />
    <PackageReference Include="NLog" Version="4.4.4" />
  </ItemGroup>

</Project>